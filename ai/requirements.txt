fastapi>=0.115.0
uvicorn[standard]>=0.30.6
langchain>=0.3.4
langchain-openai>=0.2.2
python-dotenv>=1.0.1
# 主对话链路直接 import openai（原生 SDK 保留 reasoning_content 厂商扩展），显式声明
openai>=1.40.0
# 阶段1：只读工具调用循环（StateGraph + custom stream writer）
langgraph>=1.2.11

# 阶段2：工作区语义检索（RAG）——fastembed(ONNX 本地编码，免 torch) + numpy(向量运算)
fastembed>=0.8.0
numpy>=2.5.3

# 阶段2：工作区 PDF/Markdown 文档读取 + 文件上传
PyMuPDF>=1.24.0
python-multipart>=0.0.9

# 阶段4A：MCP client（接入外部 MCP server，stdio 传输；下限锁 venv 实测版本）
mcp>=2.2.0

# 测试（tests/）
pytest>=8.0.0
